<ul class="nav">
  <li>
    <%= link_to "Main", root_path, :class => 'brand' %>
  </li>
  <li>
    <%= link_to 'My Library', '' %>
  </li>
  <% if logged_in? %>
    
  <% else %>
   
  <% end %>
</ul>

<% if logged_in? %>
  <div class='nav_item'>
    <%= link_to 'Log out', logout_path, class: 'btn btn-small' %>
  </div>
<% else %>
  <div class='nav_item'>
    <%= link_to 'Register', register_path, class: 'btn btn-success btn-small' %>
  </div>
  <div class='nav_item'>
      <%= link_to 'Log in', login_path, class: 'btn btn-small' %>
  </div>
<% end %>
